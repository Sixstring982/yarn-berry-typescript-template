# Yarn Berry TypeScript template

This change is a template project for the browser which uses:

* TypeScript 
* Yarn Berry as the package manager with zero installs
* esbuild for bundling
* SASS with CSS modules for styling
* Jest for testing
